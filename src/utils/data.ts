import {Vector3D} from './math';

const orientationThreshold = 0.1;

const isStraight = (acceleration: Vector3D, orientaion: Vector3D): boolean => {
  //   return (acceleration.x <= -3 && );
  return;
};

const isHook = (acceleration: Vector3D): boolean => {
  return true;
};

const isUpperCut = (acceleration: Vector3D): boolean => {
  return true;
};

const isBody = (acceleration: Vector3D): boolean => {
  return true;
};
